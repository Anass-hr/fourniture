{
  "name": "officestock-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "prisma generate && prisma migrate deploy && node src/seed.js",
    "migrate": "prisma migrate dev",
    "seed": "node src/seed.js",
    "reset": "node src/reset.js",
    "export": "node src/export-data.js",
    "import": "node src/import-data.js",
    "dev": "node --watch src/server.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
