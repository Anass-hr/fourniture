{
  "name": "officestock",
  "version": "1.0.0",
  "private": true,
  "description": "OfficeStock — gestion d'inventaire des fournitures de bureau",
  "scripts": {
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "setup": "npm --prefix backend run setup",
    "reset": "npm --prefix backend run reset",
    "dev": "concurrently -n API,WEB -c blue,green \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix backend run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
